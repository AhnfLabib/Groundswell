# Copy this file to .dev.vars for local Worker secrets (wrangler dev reads this)
# No secrets needed for local dev — all config lives in wrangler.toml [vars]
# Add any sensitive values here if you later add auth or API keys
